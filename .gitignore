# Dependencies
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Chrome Browser Data & Profiles
chrome_profile/

# Agent Metadata
.agents/

# Logs
*.log
logs/

# Uploads & Downloads (large binary files, mount via volume)
uploads/*
!uploads/.gitkeep
downloads/*
!downloads/.gitkeep

# System and Temp Files
.DS_Store
Thumbs.db
*.tmp
*.bak
backups/
.env
.env.*
!.env.example

# GTF Video AI Automation V2 (he thong moi)
byteplus_queue_db.json
byteplus_kol_library.json
byteplus_mock_jobs.json
byteplus_usage.json
*.backup.json
byteplus_uploads/
byteplus_outputs/
*.corrupt-*
server.js.pre-v2.bak
*.err

# Video to Video tool — local state
video_studio_library.json
video_studio_projects.json

# Kho video nguồn (Video to Video) — không commit file media
/kho/*
!/kho/.gitkeep

# Video đối thủ (reference) — không commit
/video_studio_references/

# Thumbnail cache (Video to Video)
/video_studio_thumbs/

# Flowq (Google Flow qua Chrome CDP 9334) — output/upload/db cục bộ
/flow_outputs/
