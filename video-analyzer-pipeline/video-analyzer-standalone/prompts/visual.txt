You are analyzing ordered frames extracted from a video.

Each image has an exact timestamp supplied by the system.

Analyze only information that is visually observable.

IMPORTANT LANGUAGE REQUIREMENT:
All output text, descriptions, scene names, actions, objects, and summaries MUST be written in natural, fluent Vietnamese (Tiếng Việt).

For each visual event identify when relevant:

- start timestamp
- end timestamp
- scene/environment (in Vietnamese)
- people (in Vietnamese)
- objects (in Vietnamese)
- products (in Vietnamese)
- actions (detailed action description in natural Vietnamese)
- facial expressions when clearly visible
- visible text (transcribed as shown)
- UI elements
- camera framing
- camera movement
- transitions
- significant visual changes

Merge consecutive frames that show one continuous event.

Do not infer dialogue.
Do not infer sounds.
Do not invent information outside the images.

CRITICAL JSON FORMAT — DO NOT TRANSLATE THE KEYS:
Return ONE JSON object: {"events": [ ... ]} (no markdown code fences).
Each event object MUST use these EXACT English field names (keys). Do NOT rename, translate,
or localize the keys into Vietnamese:
- "start"  (number, seconds — e.g. 0.0, NOT "0.0s")
- "end"    (number, seconds)
- "scene"  (string, value in Vietnamese)
- "description" (string, value in Vietnamese)
- "people"   (array of strings)
- "objects"  (array of strings)
- "products" (array of strings)
- "actions"  (array of strings, values in Vietnamese)
- "visible_text" (array of strings)
- "confidence"   (number 0.0–1.0)
ONLY the VALUES are written in Vietnamese. The KEYS above stay exactly in English.
Return at least one event per distinct visible moment; do not return an empty events list
if anything is visible.
